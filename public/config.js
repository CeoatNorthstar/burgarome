// Backend (Cloudflare Worker) base URL.
//
// Leave empty for local development with `npm start` (same-origin server).
// After `wrangler deploy`, set this to your Worker URL, e.g.:
//   window.BURGAROME_BACKEND = "https://burgarome-signaling.YOUR-SUBDOMAIN.workers.dev";
window.BURGAROME_BACKEND = "https://burgarome-signaling.a-contactnaol.workers.dev";
